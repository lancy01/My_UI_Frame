pytest的assert断言
支持表达式，如== in is等
常用断言：pytest里面的断言实际上就是python里面assert的断言方法，常用以下几种：
·assert xx 判断xx为真
·assert not xx 判断xx不为真
·assert a in b 判断b包含a
·assert a == b 判断a等于b
·assert a >= b 判断a大于等于b
·assert a <= b 判断a小于等于b
·assert a != b 判断a不等于b
